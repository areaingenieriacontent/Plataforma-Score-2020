@using SCORM1.Models.Lms;
@using SCORM1.Models.ViewModel;
@using SCORM1.Models.RigidCourse;
@model AdminProtectedFailure

@{
    ViewBag.Title = "Falla Protegida";
}

<h2>Falla Protegida</h2>

<div class="btn-group btn-breadcrumb">
    @*<a href="@Url.Action("Index", "Home")" class="btn btn-info"><i class="glyphicon glyphicon-home"></i></a>
    <a href="@Url.Action("Grades", "AdminTraining", new {id = Model.protectedFailureTest.Modu_Id})" class="btn btn-info">CURSO</a>*@
</div>
<br />
<br />
<div class="panel panel-primary">
    <div id="DeleteInfo" class="modal fade" role="dialog">
        <div class="modal-dialog">
            <div class="modal-content">
                <div class="modal-header">
                    <button type="button" class="close" data-dismiss="modal">&times;</button>
                    <h4 class="modal-title"></h4>
                </div>
                <div class="modal-body">
                    @TempData["Add"]
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-default" data-dismiss="modal">Cerrar</button>
                </div>
            </div>
        </div>
    </div>
    <div class="panel-heading panel-head"><center><h1 style="font-size:35px;">Categorías</h1></center></div>
</div>